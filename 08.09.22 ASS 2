#include<stdio.h>
#include<conio.h>
int main()
{
	int a[10],*p,n,i,sum=0;
	scanf("%d",&n);
	printf("enter array elements");
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	p=&a[0];
	for(i=0;i<n;i++)
	{
		sum=sum+(*p);
		p++;
	}
	printf("%d",sum);
	return(0);
}
